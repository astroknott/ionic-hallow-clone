import { ImageShrinkDirective } from './image-shrink.directive';

describe('ImageShrinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageShrinkDirective();
    expect(directive).toBeTruthy();
  });
});
